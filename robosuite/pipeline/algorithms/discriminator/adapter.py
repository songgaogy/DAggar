"""Read-only adapter to the upstream `robosuite.discriminator.lpb_v2` package.

Reserved for thin re-exports / type aliases that DIPOLE-RL code needs but
shouldn't import directly from the standalone lpb_v2 package. Implementation
is intentionally empty for now; the online-trainable head lives in
`online_bce.py` and the shared encoder lives in `encoder.py`.
"""
