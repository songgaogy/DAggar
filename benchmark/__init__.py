"""Top-level benchmark package.

Two source-specific skins share a generic core:
    benchmark.core         - source-agnostic FailureBenchmark, metrics, protocol.
    benchmark.robosuite    - robosuite HDF5 loader (mirrors data/utils/benchmark).
    benchmark.real_world   - agilex HDF5 loader.
"""
